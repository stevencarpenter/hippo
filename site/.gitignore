node_modules/
dist/
.astro/
.env
.env.production
.DS_Store
*.log
.pagefind/

# TypeScript / Vite / Vitest caches and incremental build artefacts.
*.tsbuildinfo
.vite/
.vitest/
.cache/

# Coverage and test reports — vitest run with --coverage emits these.
coverage/
.nyc_output/

# Playwright / linkinator artifacts — kept locally in /tmp during runs,
# but commit these patterns defensively so accidental --reporter=html
# output never lands in the tree.
playwright-report/
test-results/
linkinator-report.json

# Generated by copy-doc-images integration on every build — repopulated from ../docs/diagrams.
public/docs-images/

# Generated by copy-public-assets integration — source of truth is ../scripts/install.sh.
public/install.sh
