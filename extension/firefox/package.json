{
  "private": true,
  "name": "hippo-browser-capture",
  "version": "0.2.0",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "bun test tests/",
    "typecheck": "tsc --noEmit",
    "package": "node build.mjs && npx web-ext build --overwrite-dest --ignore-files src/ build.mjs tsconfig.json package.json package-lock.json README.md"
  },
  "devDependencies": {
    "esbuild": "^0.25",
    "typescript": "^5.8",
    "web-ext": "^10.1.0"
  }
}
